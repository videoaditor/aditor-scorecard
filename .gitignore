node_modules/
dashboard/node_modules/
dashboard/dist/
**/.env
*.log
.devcontainer/
new-client.sh
README.md

# dashboard/.env.test is intentionally NOT ignored (the `**/.env` rule above does
# not match it) - it holds only non-secret placeholders and must be committed,
# because `npm run build -- --mode test` is the gate and reads it.

# Vite dep-optimizer cache (transient, created by any local build)
dashboard/.vite/
