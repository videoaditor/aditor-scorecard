{
  "name": "aditor-scorecard-ig-collector",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Weekly Cloudflare Worker: collects Instagram organic reach + classified hot-DM count and upserts them into the Aditor Scorecard Teable weekly row (aligned with the n8n cadence).",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "vitest": "^4.1.9"
  }
}
